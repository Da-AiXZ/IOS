# Meson cross-compilation file for iOS ARM64
# @SDKROOT@ is replaced by CI workflow with the actual SDK path

[binaries]
c = 'clang'
ar = 'ar'
strip = 'strip'

[host_machine]
system = 'darwin'
cpu_family = 'aarch64'
cpu = 'aarch64'
endian = 'little'

[built-in options]
c_args = ['-arch', 'arm64', '-isysroot', '@SDKROOT@', '-miphoneos-version-min=16.0']
c_link_args = ['-arch', 'arm64', '-isysroot', '@SDKROOT@', '-miphoneos-version-min=16.0']

[properties]
needs_exe_wrapper = true
