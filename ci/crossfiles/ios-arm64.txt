# Meson cross-compilation for iOS ARM64
# SDKROOT and CC must be set in environment before running meson

[binaries]
c = 'clang'
ar = 'ar'
strip = 'strip'

[host_machine]
system = 'darwin'
cpu_family = 'aarch64'
cpu = 'aarch64'
endian = 'little'

[built-in options]
c_args = ['-arch', 'arm64', '-isysroot', '@SDKROOT@', '-miphoneos-version-min=16.0']
c_link_args = ['-arch', 'arm64', '-isysroot', '@SDKROOT@', '-miphoneos-version-min=16.0']

[properties]
needs_exe_wrapper = true
